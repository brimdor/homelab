app-template:
  controllers:
    main:
      containers:
        main:
          image:
            repository: docker.io/brimdor/family-games-host-queue-cards
            tag: latest
            pullPolicy: Always
          env:
            TZ: "America/Chicago"
  service:
    main:
      controller: main
      primary: true
      ports:
        http:
          port: 80
          protocol: HTTP
  ingress:
    main:
      enabled: true
      className: nginx
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt-prod
      hosts:
        - host: family-games-host.eaglepass.io
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: 80
      tls:
        - hosts:
            - family-games-host.eaglepass.io
          secretName: family-games-host-tls-certificate
