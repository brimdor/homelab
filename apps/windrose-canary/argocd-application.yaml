apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: windrose-canary
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://git.eaglepass.io/ops/homelab.git
    targetRevision: master
    path: apps/windrose-canary
  destination:
    server: https://kubernetes.default.svc
    namespace: windrose-canary
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
