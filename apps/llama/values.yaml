app-template:
  controllers:
    main:
      replicas: 1
      initContainers:
        check-model-ready:
          image:
            repository: alpine
            tag: latest
          env:
            LLAMA_ARG_MODEL:
              value: /models/Qwen3.6-28B-REAP20-A3B-Q4_K_M.gguf
          command:
            - /bin/sh
            - -c
          args:
            - |
              if [ -s "$LLAMA_ARG_MODEL" ]; then
                echo "Model present: $LLAMA_ARG_MODEL"
                ls -lh "$LLAMA_ARG_MODEL"
                exit 0
              fi
              echo "Waiting for model to be downloaded..."
              until [ -s "$LLAMA_ARG_MODEL" ]; do
                echo "  - $(stat -c%s "$LLAMA_ARG_MODEL" 2>/dev/null || echo 'missing')"
                sleep 10
              done
              echo "Model ready"
      containers:
        main:
          image:
            repository: ghcr.io/ggml-org/llama.cpp
            tag: server-cuda12
          env:
            TZ: America/Chicago
            PUID: "1000"
            PGID: "1000"
            LLAMA_ARG_HOST: 0.0.0.0
            LLAMA_ARG_PORT: "8080"
            LLAMA_ARG_MODEL: /models/Qwen3.6-28B-REAP20-A3B-Q4_K_M.gguf
            LLAMA_ARG_CTX_SIZE: "32768"
            LLAMA_ARG_N_PARALLEL: "2"
            LLAMA_ARG_N_GPU_LAYERS: "99"
            LLAMA_ARG_WEBUI: "true"
            NVIDIA_VISIBLE_DEVICES: "all"
            NVIDIA_DRIVER_CAPABILITIES: "all"
            NVIDIA_DISABLE_REQUIRE: "true"
          resources:
            requests:
              cpu: 2000m
              memory: 16Gi
              nvidia.com/gpu: 1
            limits:
              memory: 24Gi
              nvidia.com/gpu: 1

    turboquant:
      replicas: 0
      type: deployment
      initContainers:
        check-model-ready:
          image:
            repository: alpine
            tag: latest
          env:
            LLAMA_ARG_MODEL:
              value: /models/Qwen3.6-28B-REAP20-A3B-Q4_K_M.gguf
          command:
            - /bin/sh
            - -c
          args:
            - |
              if [ -s "$LLAMA_ARG_MODEL" ]; then
                echo "Model present: $LLAMA_ARG_MODEL"
                ls -lh "$LLAMA_ARG_MODEL"
                exit 0
              fi
              echo "Waiting for model to be downloaded..."
              until [ -s "$LLAMA_ARG_MODEL" ]; do
                echo "  - $(stat -c%s "$LLAMA_ARG_MODEL" 2>/dev/null || echo 'missing')"
                sleep 10
              done
              echo "Model ready"
      containers:
        turboquant:
          image:
            repository: vito974/llama-cpp-turboquant
            tag: server-cuda12
          env:
            TZ: America/Chicago
            PUID: "1000"
            PGID: "1000"
            LLAMA_ARG_HOST: 0.0.0.0
            LLAMA_ARG_PORT: "8081"
            LLAMA_ARG_MODEL: /models/Qwen3.6-28B-REAP20-A3B-Q4_K_M.gguf
            LLAMA_ARG_CTX_SIZE: "32768"
            LLAMA_ARG_N_PARALLEL: "2"
            LLAMA_ARG_N_GPU_LAYERS: "99"
            NVIDIA_VISIBLE_DEVICES: "all"
            NVIDIA_DRIVER_CAPABILITIES: "all"
            NVIDIA_DISABLE_REQUIRE: "true"
          args:
            - --server
            - --model
            - /models/Qwen3.6-28B-REAP20-A3B-Q4_K_M.gguf
            - --ctx-size
            - "32768"
            - --n-gpu-layers
            - "99"
            - --host
            - 0.0.0.0
            - --port
            - "8081"
          resources:
            requests:
              cpu: 2000m
              memory: 16Gi
              nvidia.com/gpu: 1
            limits:
              memory: 24Gi
              nvidia.com/gpu: 1

    downloader:
      replicas: 1
      pod:
        securityContext:
          fsGroup: 1000
        tolerations:
          - key: dedicated
            operator: Equal
            value: arcanine
            effect: NoSchedule
        affinity:
          nodeAffinity:
            requiredDuringSchedulingIgnoredDuringExecution:
              nodeSelectorTerms:
                - matchExpressions:
                    - key: kubernetes.io/hostname
                      operator: In
                      values:
                        - "arcanine"
      containers:
        downloader:
          image:
            repository: curlimages/curl
            tag: 8.20.0
          command:
            - /bin/sh
            - -c
          args:
            - |
              set -eu
              mkdir -p /models

              download_model() {
                url="$1"
                dest="$2"
                if [ -s "$dest" ]; then
                  echo "Model already present: $dest"
                  ls -lh "$dest"
                  return 0
                fi

                echo "Downloading $dest"
                curl -L --fail --retry 5 --retry-delay 5 -C - \
                  -o "${dest}.part" \
                  "$url"
                mv "${dest}.part" "$dest"
                echo "Downloaded: $dest"
                ls -lh "$dest"
              }

              download_model "https://huggingface.co/barozp/Qwen3.6-28B-REAP20-A3B-GGUF/resolve/main/Qwen3.6-28B-REAP20-A3B-Q4_K_M.gguf" \
                "/models/Qwen3.6-28B-REAP20-A3B-Q4_K_M.gguf"

              download_model "https://huggingface.co/unsloth/GLM-4.7-Flash-REAP-23B-A3B-GGUF/resolve/main/GLM-4.7-Flash-REAP-23B-A3B-Q4_K_M.gguf" \
                "/models/GLM-4.7-Flash-REAP-23B-A3B-Q4_K_M.gguf"

              echo "All requested models are present; keeping downloader pod alive"
              exec tail -f /dev/null

  defaultPodOptions:
    runtimeClassName: nvidia
    hostAliases:
      - ip: "143.204.130.38"
        hostnames:
          - huggingface.co
    tolerations:
      - key: dedicated
        operator: Equal
        value: arcanine
        effect: NoSchedule
    affinity:
      nodeAffinity:
        requiredDuringSchedulingIgnoredDuringExecution:
          nodeSelectorTerms:
            - matchExpressions:
                - key: kubernetes.io/hostname
                  operator: In
                  values:
                    - "arcanine"

  service:
    main:
      controller: main
      primary: true
      ports:
        http:
          port: 8080
          protocol: HTTP
    turboquant:
      controller: turboquant
      ports:
        http:
          port: 8081
          protocol: HTTP

  ingress:
    main:
      enabled: true
      className: nginx
      annotations:
        external-dns.alpha.kubernetes.io/target: homelab-tunnel.eaglepass.io
        external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
        cert-manager.io/cluster-issuer: letsencrypt-prod
      hosts:
        - host: &host llama.eaglepass.io
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: main
                port: http
      tls:
        - hosts:
            - *host
          secretName: llama-tls
    turboquant:
      enabled: true
      className: nginx
      annotations:
        external-dns.alpha.kubernetes.io/target: homelab-tunnel.eaglepass.io
        external-dns.alpha.kubernetes.io/cloudflare-proxied: "true"
        cert-manager.io/cluster-issuer: letsencrypt-prod
      hosts:
        - host: &tqhost turboquant.eaglepass.io
          paths:
            - path: /
              pathType: Prefix
              service:
                identifier: turboquant
                port: http
      tls:
        - hosts:
            - *tqhost
          secretName: turboquant-tls

  persistence:
    data:
      accessMode: ReadWriteOnce
      size: 50Gi
      advancedMounts:
        main:
          check-model-ready:
            - path: /models
          main:
            - path: /models
        turboquant:
          check-model-ready:
            - path: /models
          turboquant:
            - path: /models
        downloader:
          downloader:
            - path: /models
