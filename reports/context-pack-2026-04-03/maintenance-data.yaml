title: "[Maintenance] 2026-03-31 - Homelab"
date: "2026-03-31"

status:
  overall: "YELLOW"
  last_updated: "2026-04-03 18:35 CDT"
  source_report: "reports/status-report-2026-04-03.md"
  assigned_to: "gitea_admin"

cluster:
  k3s_version: "v1.33.6+k3s1"
  client_version: "v1.35.3"
  node_count: 10
  app_count: 48
  ceph_status: "HEALTH_WARN"

layers:
  metal:
    nodes:
      status: "GREEN"
      details: "10/10 Ready (arcanine, bulbasaur, charmander, chikorita, cyndaquil, growlithe, pikachu, sprigatito, squirtle, totodile)"
    versions:
      status: "GREEN"
      details: "All nodes on v1.33.6+k3s1"
    cni:
      status: "GREEN"
      details: "Cilium 10/10 Ready, operator 1/1 Running"
    kured:
      status: "GREEN"
      details: "ArgoCD app kured Synced+Healthy"

  system:
    coredns:
      status: "GREEN"
      details: "CoreDNS 1/1 Running; cluster DNS reachable"
    metrics_server:
      status: "GREEN"
      details: "metrics-server 1/1 Running; node metrics available"
    kube_vip:
      status: "GREEN"
      details: "kube-vip running on bulbasaur, charmander, and squirtle"
    argocd:
      status: "YELLOW"
      details: "46/48 apps Synced+Healthy; rook-ceph Degraded and scribe Progressing (scribe handled separately)"

  storage:
    ceph_health:
      status: "YELLOW"
      details: "HEALTH_WARN due 2 recent mgr module crashes (prometheus + rook) on Ceph v20.2.0; all data services active+clean"
    osds:
      status: "GREEN"
      details: "6 OSDs up, 6 in across 5 hosts"
    usage:
      status: "YELLOW"
      details: "Approx. 1.4 TiB used of 3.0 TiB raw; standard-rwo pool about 55.17% used"
    pools:
      status: "GREEN"
      details: "4 pools, 177 PGs, all active+clean"
    monitors:
      status: "GREEN"
      details: "3 mons in quorum (h,k,l), leader h"
    mds:
      status: "GREEN"
      details: "1 active MDS with 1 hot standby"

  platform:
    ingress:
      status: "GREEN"
      details: "Ingress-nginx healthy; 33 ingresses present"
    certs:
      status: "GREEN"
      details: "33/33 certificates Ready=True"
    secrets:
      status: "GREEN"
      details: "7/7 ExternalSecrets Ready=True; secret stores accessible"
    cert_manager:
      status: "GREEN"
      details: "cert-manager flow healthy; no stuck orders/challenges at snapshot time"

  apps:
    pods:
      status: "YELLOW"
      details: "No CrashLoopBackOff/Error/ImagePullBackOff pods; scribe rollout is Progressing under separate active remediation"
    gitea:
      status: "GREEN"
      details: "gitea, PostgreSQL, and Valkey pods all Running"
    grafana:
      status: "GREEN"
      details: "grafana pod 3/3 Running"
    kanidm:
      status: "GREEN"
      details: "kanidm-0 1/1 Running"

observations:
  node_activity: "10 nodes stable. CPU usage ranges roughly 3-36%, memory 9-26%. No node pressure detected. Dedicated taints remain on arcanine and sprigatito."
  renovate: "Open PRs: #80 chore(deps) non-major dependency bundle, #81 bitnamilegacy/postgresql tag to v17. Dependency Dashboard #4 also reports missing GitHub token/release-note coverage, docker lookup failures for podwave images, and blocked nextcloud v9 recreation (#74)."
  network:
    workstation: "GREEN - direct kubectl access to https://10.0.20.50:6443 from workstation"
    gitea: "GREEN - API token authenticated as gitea_admin"
    nas: "GREEN - Unraid reachable on SMB/NFS/Web checks"
    gateway: "GREEN - all VLAN gateways reachable"

proposed_changes:
  - id: "B1"
    type: "Remediation"
    layer: "Storage"
    priority: "P0"
    impact: "Low"
    downtime: "None"
    summary: "Restore Ceph HEALTH_OK by validating mgr stability and archiving only historical crash records if no fresh crashes remain"
    dependencies: ["None"]
  - id: "B2"
    type: "Upgrade"
    layer: "Storage"
    priority: "P0"
    impact: "Medium"
    downtime: "Maintenance window"
    summary: "Upgrade Rook Ceph image off known-bad Ceph v20.2.0 to a fixed supported 20.2.x patch and validate chart compatibility"
    dependencies: ["B1"]
  - id: "C1"
    type: "Configuration"
    layer: "Platform"
    priority: "P1"
    impact: "Low"
    downtime: "None"
    summary: "Re-enable rook-ceph monitoring once the Ceph patch is verified stable"
    dependencies: ["B2"]
  - id: "C2"
    type: "Investigation"
    layer: "Storage"
    priority: "P1"
    impact: "Medium"
    downtime: "Host maintenance may be required"
    summary: "Investigate bulbasaur OSD hardware exclusion and recurring BlueFS stalled-read history"
    dependencies: ["None"]
  - id: "C3"
    type: "Investigation"
    layer: "Apps"
    priority: "P1"
    impact: "Low"
    downtime: "None"
    summary: "Investigate Woodpecker SQLite lock errors causing intermittent health probe failures"
    dependencies: ["None"]
  - id: "D1"
    type: "Review"
    layer: "Platform"
    priority: "P2"
    impact: "Medium"
    downtime: "None"
    summary: "Review and decide merge/close for PR #80 (non-major dependency bundle) with staged validation"
    dependencies: ["None"]
  - id: "D2"
    type: "Review"
    layer: "Apps"
    priority: "P2"
    impact: "Medium"
    downtime: "Possible app-specific restart"
    summary: "Review PR #81 (bitnamilegacy/postgresql v17) with release-note check, backup, and staged rollout plan"
    dependencies: ["Database backup"]
  - id: "D3"
    type: "Maintenance"
    layer: "System"
    priority: "P2"
    impact: "Low"
    downtime: "None"
    summary: "Fix Renovate Dependency Dashboard drift and token/registry lookup problems, then refresh issue #4"
    dependencies: ["None"]
  - id: "D4"
    type: "Decision"
    layer: "Apps"
    priority: "P2"
    impact: "Medium"
    downtime: "Maintenance window if executed"
    summary: "Review blocked nextcloud v9 dashboard item and decide whether to recreate PR #74"
    dependencies: ["Release notes review"]
  - id: "E1"
    type: "Monitoring"
    layer: "Storage"
    priority: "P3"
    impact: "Low"
    downtime: "None"
    summary: "Track Ceph raw and standard-rwo usage trends and plan capacity before they become urgent"
    dependencies: ["None"]

action_items:
  - id: "A1"
    priority: "P0"
    layer: "System"
    phase: "A"
    title: "Refresh cluster baseline before maintenance starts"
    goal: "Confirm the live starting point for nodes, ArgoCD apps, Ceph, network, and NAS before making any changes"
    commands: "kubectl get nodes -o wide; kubectl get applications -n argocd; kubectl -n rook-ceph exec deploy/rook-ceph-tools -- ceph health detail; ~/.config/opencode/scripts/homelab-network-check.sh --verbose; ~/.config/opencode/scripts/homelab-nas-check.sh --verbose"
    expected: "Live baseline captured and any drift from this recon identified before action work begins"
    if_fails: "Stop and rerun /homelab-recon if the starting state materially differs from this report"
    rollback: "N/A"
  - id: "B1"
    priority: "P0"
    layer: "Storage"
    phase: "B"
    title: "Clear current Ceph HEALTH_WARN state if only historical mgr crashes remain"
    goal: "Return Ceph and the rook-ceph ArgoCD app to GREEN if current warnings are only retained historical crash records"
    commands: "kubectl -n rook-ceph exec deploy/rook-ceph-tools -- ceph health detail; kubectl -n rook-ceph exec deploy/rook-ceph-tools -- ceph crash ls-new; kubectl -n rook-ceph exec deploy/rook-ceph-tools -- ceph mgr services; kubectl get application rook-ceph -n argocd; kubectl -n rook-ceph exec deploy/rook-ceph-tools -- ceph crash archive-all"
    expected: "ceph health becomes HEALTH_OK and ArgoCD rook-ceph becomes Healthy without any new mgr crashes appearing"
    if_fails: "If fresh mgr crashes continue or services disappear, stop and continue with B2 as the durable fix path"
    rollback: "N/A"
  - id: "B2"
    priority: "P0"
    layer: "Storage"
    phase: "B"
    title: "Upgrade Ceph off v20.2.0 and validate Rook compatibility"
    goal: "Eliminate the known Ceph v20.2.0 mgr/orchestrator bug that is causing recurring rook-ceph instability"
    commands: "grep -n \"cephVersion\\|image:\" system/rook-ceph/values.yaml; grep -n \"version:\" system/rook-ceph/Chart.yaml; helm dependency update system/rook-ceph; kubectl get application rook-ceph -n argocd; kubectl -n rook-ceph exec deploy/rook-ceph-tools -- ceph health"
    expected: "Repo pinned to a fixed supported Ceph patch, sync completes cleanly, ceph health is HEALTH_OK, and rook-ceph becomes Synced+Healthy"
    if_fails: "Review upstream Rook/Ceph compatibility notes, hold the rollout, and revert to the previous known-good chart/image combination before retrying"
    rollback: "Revert the Ceph image/chart bump commit, resync rook-ceph, and verify ceph health plus ArgoCD health return to the pre-change state"
  - id: "C1"
    priority: "P1"
    layer: "Platform"
    phase: "C"
    title: "Re-enable rook-ceph monitoring after the Ceph patch is stable"
    goal: "Restore native Ceph monitoring once the mgr crash bug is no longer present"
    commands: "grep -n \"monitoring:\\|enabled:\" system/rook-ceph/values.yaml; kubectl -n rook-ceph exec deploy/rook-ceph-tools -- ceph mgr services; kubectl get prometheusrule -n rook-ceph; kubectl get application rook-ceph -n argocd"
    expected: "rook-ceph monitoring is enabled in GitOps and the mgr prometheus endpoint remains stable without new crashes"
    if_fails: "Disable monitoring again and capture mgr logs/crash info before proceeding"
    rollback: "Set rook-ceph-cluster.monitoring.enabled back to false, resync rook-ceph, and confirm mgr stability"
  - id: "C2"
    priority: "P1"
    layer: "Storage"
    phase: "C"
    title: "Investigate the excluded bulbasaur OSD device and decide repair vs permanent removal"
    goal: "Resolve the long-running BlueFS stalled-read risk that already forced bulbasaur out of OSD placement"
    commands: "grep -n \"bulbasaur excluded\\|BlueFS\" system/rook-ceph/values.yaml; kubectl -n rook-ceph exec deploy/rook-ceph-tools -- ceph osd tree; kubectl -n rook-ceph exec deploy/rook-ceph-tools -- ceph health detail; kubectl -n rook-ceph logs deploy/rook-ceph-operator --tail=200"
    expected: "Documented decision to repair/reintroduce the device or keep it permanently excluded with clear rationale"
    if_fails: "Escalate to host-level disk diagnostics and schedule node maintenance before making cluster storage changes"
    rollback: "N/A - investigation and planning only"
  - id: "C3"
    priority: "P1"
    layer: "Apps"
    phase: "C"
    title: "Investigate woodpecker-server SQLite database lock errors"
    goal: "Determine whether Woodpecker needs backend changes or workload tuning to avoid intermittent probe failures"
    commands: "kubectl logs -n woodpecker woodpecker-server-0 --tail=200; kubectl describe pod -n woodpecker woodpecker-server-0; kubectl get pvc -n woodpecker; kubectl get statefulset -n woodpecker woodpecker-server -o yaml"
    expected: "Root cause documented and a clear remediation path chosen (tuning, storage change, or PostgreSQL migration)"
    if_fails: "Capture previous logs and create a dedicated follow-up task for PostgreSQL migration planning"
    rollback: "N/A - investigation only"
  - id: "D1"
    priority: "P2"
    layer: "Platform"
    phase: "D"
    title: "Review and decide PR #80 (all non-major dependencies)"
    goal: "Safely move routine dependency updates that are already prepared by Renovate"
    commands: "curl -s https://git.eaglepass.io/api/v1/repos/ops/homelab/pulls/80; git log --oneline --decorate -n 20; pre-commit run --all-files"
    expected: "PR #80 is either merged after validation or closed with documented reason"
    if_fails: "Split high-risk updates out of the bundle or defer the PR with explicit blockers"
    rollback: "Revert the merged dependency commit/PR and resync affected apps if regressions appear"
  - id: "D2"
    priority: "P2"
    layer: "Apps"
    phase: "D"
    title: "Review and stage PR #81 (bitnamilegacy/postgresql v17)"
    goal: "Handle the PostgreSQL major image change with release-note review, backup, and staged validation"
    commands: "curl -s https://git.eaglepass.io/api/v1/repos/ops/homelab/pulls/81; grep -R \"bitnamilegacy/postgresql\" -n apps platform; kubectl get pvc -A | rg postgresql; kubectl get pods -A | rg -i postgres"
    expected: "PR #81 has a documented merge/close decision with backup and validation steps before rollout"
    if_fails: "Defer the PR, capture release-note blockers, and schedule app-specific migration testing"
    rollback: "Restore the previous PostgreSQL image tag, revert the merged change, and validate database pods recover cleanly"
  - id: "D3"
    priority: "P2"
    layer: "System"
    phase: "D"
    title: "Fix Renovate dashboard drift and lookup failures"
    goal: "Get Dependency Dashboard #4 back into a trustworthy state and restore release-note/package lookup coverage"
    commands: "curl -s https://git.eaglepass.io/api/v1/repos/ops/homelab/issues/4; kubectl exec -n renovate deploy/renovate -- printenv | rg 'GITHUB|TOKEN'; kubectl logs -n renovate deploy/renovate --tail=200"
    expected: "Dependency Dashboard reflects real PR state and Renovate warnings about GitHub token/docker lookups are either fixed or explicitly documented"
    if_fails: "Document the exact credential or registry lookup failure and keep dashboard refresh as a separate follow-up"
    rollback: "N/A"
  - id: "D4"
    priority: "P2"
    layer: "Apps"
    phase: "D"
    title: "Decide whether to recreate blocked nextcloud v9 PR #74"
    goal: "Make an explicit maintenance decision on the blocked major Nextcloud update instead of leaving it ambiguous in the dashboard"
    commands: "curl -s https://git.eaglepass.io/api/v1/repos/ops/homelab/issues/4; grep -R \"nextcloud\" -n apps/nextcloud; kubectl get pods -n nextcloud -o wide"
    expected: "Issue #4 and the maintenance issue both record a clear recreate/defer decision with rationale"
    if_fails: "Defer with documented blockers and required release-note testing steps"
    rollback: "N/A - decision gate only"
  - id: "E1"
    priority: "P3"
    layer: "Storage"
    phase: "E"
    title: "Track Ceph capacity and pool usage trends"
    goal: "Prevent current 55% standard-rwo usage from becoming an urgent storage event later"
    commands: "kubectl -n rook-ceph exec deploy/rook-ceph-tools -- ceph df detail; kubectl -n rook-ceph exec deploy/rook-ceph-tools -- ceph osd df tree; kubectl get pvc -A -o custom-columns='NAMESPACE:.metadata.namespace,NAME:.metadata.name,SIZE:.spec.resources.requests.storage'"
    expected: "Capacity trend documented with thresholds for expansion or cleanup before health degrades"
    if_fails: "If usage is accelerating or any pool nears a warning threshold, promote storage expansion planning to P1"
    rollback: "N/A - monitoring and planning only"
  - id: "F1"
    priority: "P0"
    layer: "System"
    phase: "F"
    title: "Run full post-maintenance validation gate"
    goal: "Confirm the homelab returns to GREEN across all layers after each significant maintenance change"
    commands: "kubectl get nodes; kubectl get pods -n kube-system; kubectl get applications -n argocd; kubectl -n rook-ceph exec deploy/rook-ceph-tools -- ceph health; kubectl get pods -A; ~/.config/opencode/scripts/homelab-network-check.sh --verbose; ~/.config/opencode/scripts/homelab-nas-check.sh --verbose"
    expected: "All nodes Ready, Ceph HEALTH_OK, ArgoCD apps Synced+Healthy, no bad pod states, network GREEN, NAS GREEN"
    if_fails: "Stop immediately on the first non-GREEN layer and return to targeted diagnostics before proceeding"
    rollback: "Undo the most recent change before continuing"

change_log:
  - timestamp: "2026-03-31 17:30"
    phase: "Recon"
    item: "Issue #79"
    action: "Initial maintenance issue created"
    result: "Baseline maintenance backlog captured"
    status_after: "YELLOW"
  - timestamp: "2026-04-03 18:35"
    phase: "Recon"
    item: "Issue #79"
    action: "Evidence refreshed and Ceph/Rook maintenance plan expanded"
    result: "Issue updated with live storage warnings, open PRs, and Renovate backlog"
    status_after: "YELLOW"

closure:
  status: ""
  started: ""
  completed: ""
  duration: ""
  resolved_by: ""
